{
  "name": "draiver-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end tests for the read-only draiver web UI",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "@types/node": "^20.16.0"
  }
}
